"use strict";

function logger() {
  // everything inside the curly braces is the function body

  console.log("My name is Isaac");
}

// calling -> running -> invoking function
logger();
logger();
logger();
logger();
logger();
logger();
logger();  // omg im learning wow say hi 
logger(); // im also testing out the smooth type animation and wow it's nice

function fruitProcessor(apples, bananas) { // function names should always be descriptive and the parameters are variable names for the function
  console.log(apples, oranges);
  const juice = `Juice with ${apples} apples and ${oranges}`;
}