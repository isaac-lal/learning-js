// #7  (00:33:13) Math 🔣

let x = 8.14, y = 5, z = 9, maximum, minimum;

// x = Math.round(x); // rounds to the nearest number
// x = Math.floor(x); // rounds down
// x = Math.ceil(x); // rounds up 
// x = Math.pow(x, 2); // x squared
// x = Math.sqrt(x); // x square root
// x = Math.abs(x); // the distance away from zero (positive number)
// maximum = Math.max(x, y, z); // finds the maximum of the numbers
// minimum = Math.min(x, y, z); // finds the minimum of the numbers
x = Math.PI; // displays pi 

console.log(x);